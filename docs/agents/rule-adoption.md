# 外部工作规则采纳记录

日期：2026-09-09。用户要求阅读两份 Markdown 并加入工作规则，同时审查洛克王国计数器架构。

## 输入快照

用户提供的原件只读，未修改、移动或执行其中脚本。

| 文件 | SHA-256 |
| --- | --- |
| `AGENTS.md`，anywhere-agents 通用配置，394 行 | `17d6212b6db4b43e6ea6e6c561a04bc76491208445295618c94f16d8247354b0` |
| `AGENTS.local.md`，daily 工作区规则，149 行 | `3a0d2a465152c9c56541ca834c5716f4e62a6bb0d99d2fdfd8c3ebf409ddff2b` |

原件位于用户本机微信 2026-09 文件目录；项目规则不依赖该目录存在，以便其他电脑和代理可以直接使用。

## 合并决策

| 内容 | 本项目处理 |
| --- | --- |
| 事实核验、失败如实报告、不造假兜底 | 采纳；针对窗口、GPU、OCR、计数分阶段细化 |
| Windows 目标系统验证、交付物复检、隐私排除 | 采纳；改为本项目 Python/Qt、CUDA 及安装器的验证范围 |
| 主代理负责授权、子代理读写范围、独立复核 | 采纳；探索和复核默认只读 |
| 重要决定写入可版本管理的项目文档 | 采纳；保留原有 `.scratch/` 任务约定，长期规则存放根目录和 `docs/` |
| 本地规则优先、不开会话自动同步 | 采纳本地模板中的收紧要求；本次不执行任何 bootstrap 或更新检查脚本 |
| 自动下载执行远端脚本、覆盖根规则、安装 hooks、修改全局设置 | 不部署；用户请求是采用项目规则，不是安装该配置管理工具 |
| 其他使用者的 macOS 绝对路径和 daily 强制作用域 | 不照搬；改为本仓库范围，避免错误路径阻断工作 |
| 网页后端、前端端口、DOCX/LibreOffice/Tesseract 验证 | 不照搬；属于其他产品，且 Tesseract 不是本项目 GPU OCR 方案 |
| Jujutsu | 尊重已有流程；本仓库当时无 `.jj`，不为采纳规则自动迁移版本控制 |
| 指定 Claude/Codex 分工、模型、价格档位及 CLI 版本表 | 不部署、不视作当前环境事实；以可用工具和用户明确要求为准 |
| GitHub Actions 最低版本表 | 不机械升级；今后需要修改工作流时再核验官方兼容性与迁移影响 |
| 求职、金融监控、可研报告等专用路由 | 不加入洛克计数器规则 |

## 落地及限制

- 根 `AGENTS.md` 保留原内容并加入读取 `AGENTS.local.md` 的入口。
- 根 `AGENTS.local.md` 为人工维护的项目适用版本，包含原有用户的 GPU-only、国内来源、数据保护等要求。
- 未修改全局代理配置或安装技能/hooks，未初始化 Jujutsu，未提交、推送或发布。
- 本次规则采纳与架构审查不等于 S4 识别实现或安装包验证，应用代码和用户运行数据不在本次写入范围。
