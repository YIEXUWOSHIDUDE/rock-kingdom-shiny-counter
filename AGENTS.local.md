# 洛克王国计数器项目规则

## 适用范围

本文件适用于本仓库的代码、文档、测试和 Windows 安装包工作，供主代理及子代理共同读取。它是根据用户提供的两份规则整理的项目版本，不是原模板的完整部署。来源、采纳内容及未采纳项见 `docs/agents/rule-adoption.md`。

保留现有 `AGENTS.md` 中的技能、任务跟踪和领域文档约定。项目需求、计划及复盘应写入本地文件；需要跨机器长期保留的决定应进入可版本管理的文档，不只保存在聊天或代理私有记忆中。

## 授权与工作范围

- “检查、审查、为什么、看看架构”授权只读诊断和新建审查记录，不自动授权修改应用或覆盖已有交付物；“实现、修复”才进入相应代码修改。
- 用户要求更新规则时可以编辑项目规则，但不得顺带修改全局 Codex/Claude 设置、模型档位、权限、系统注册表或安装其他工具。
- 不在会话启动时自动拉取远端配置、运行 bootstrap、安装依赖或升级工具。不宣称未部署的 hooks、技能或检查已经生效。
- 提交、推送、创建或合并 PR、发布 Release 和部署均须有用户明确授权。授权仅覆盖指定仓库及本次变更，不扩展到其他外部操作。
- 保留用户未提交修改和本地数据。现有仓库未使用 Jujutsu 时，不为套用模板自动初始化或迁移；已经使用时遵循该仓库现有流程。

## 产品约束

- OCR 推理只用 GPU，禁止 CPU 推理回退。普通界面、截图、数据处理等 CPU 工作不等于 CPU OCR，不应误报为违反 GPU-only。
- 只读取用户选择窗口的画面；不读游戏内存、不注入、不模拟游戏操作。
- 安装版维持离线模型及完整性校验；源代码模式需要下载模型时，优先有校验信息的国内来源。改变下载来源或校验方式需要明确说明。
- 不以降低兼容性门槛、吞掉异常或伪造“通过”来消除错误提示。硬件最低要求以项目策略文件及相应实测为依据，不从显卡名称或网上猜测推导可用性。
- 继续保留默认锁定位置、默认不注册全局快捷键和默认关闭点击穿透的产品要求；穿透必须有可靠恢复路径。
- 不自动清空或改写真实计数、历史、用户自定义关键词。配置升级要区分历史默认值与用户自定义值，并验证旧数据兼容。

## 识别与计数的证据

- 分清窗口绑定、截图、GPU 可用性、OCR 读字、目标匹配、去重及实际计数落盘。不要把后一个阶段失败笼统解释为“找不到窗口”。
- 新赛季适配先核验实际目标文字或图标，再核验完整截图中的位置、出现时机、持续时间及有效计数含义。
- 官方公告特写可以证明展示的文字或外观，不能证明 PC 全屏坐标、闪现时间或本机 OCR 命中率。
- 把事实、推断、候选方案和未验证内容分开记录。查不到、读不到、推理失败都要如实报告，不用占位结果或猜测结论冒充验证成功。
- 故障证据优先包括截图预览、阶段状态、实际 OCR 文本、计数前后值和必要日志。用户可见状态说清发生什么以及下一步；技术细节保留到诊断区。
- 保底池、事件累计数量、异色出现与清零时点只能依照已核实规则实现，不能把“重置”“结束一轮”“确认出货”未经确认地视为同一事实。

## 修改、测试和 Windows 交付

- 先跑最相关的测试，再做相应集成或构建验证。记录执行命令、结果、对应代码版本和未测试范围，不引用旧测试数量充当本轮通过。
- 对识别修改优先补充经过允许使用的实机样本和时序回放，覆盖短横幅、持续显示不重复计数、连续事件、分辨率与缩放变化。人工构造文本或假 OCR 结果不能代替真实 GPU OCR 验收。
- Windows 安装包必须验证实际交付文件及其版本、SHA-256、GPU 运行时探针、模型加载、启动、窗口捕获和计数流程。只通过源码测试、静态解压或导入检查时，不得称为 Windows 实机可用。
- 更新安装验证旧版本保留/回滚及用户数据保存；跨显卡兼容性结论要明确实测设备与仅策略覆盖的设备。
- 包内不携带密钥、账户信息、用户计数库、诊断日志、私人截图或录屏。用交付清单区分需要打包的运行库和开发虚拟环境；打包后再次核对实际内容。
- Windows 测试或后台辅助进程使用隐藏窗口，避免影响用户游戏和录制；不自动更改系统终端设置。
- 改功能或发布安装包时同步 `DEVELOPMENT.md` 的相关需求、失误修正和验证结果。不要仅更新正文而留下与本次发布矛盾的版本说明。

## 子代理协作与复核

- 仅委派输入齐全、可以独立推进的工作。每项任务交代目标、绝对项目路径、适用规则、读写范围、证据要求及停止条件。
- 主代理和子代理开始前都完整阅读本文件；主代理负责授权、规则合并、最终结论和版本控制写入。
- 同一文件只安排一个写入者。探索和审查默认只读；子代理未经主代理明确许可不再委派。
- 实现者不作为该修改的独立审查者。复核实际 diff、测试结果或交付文件，不能只复述实现者总结，也不能用多数票代替证据。
- 不委派发布、发送、提交申请、账号变更、删除或其他不可逆操作。
- 返回结果包含：完成状态、可定位证据、修改文件、检查命令与结果、未验证事项以及建议下一步。主代理核验范围和证据后再采用。

## 沟通

使用简洁中文和一致术语。先说结论，交代已验证与未验证内容；不要输出与任务无关的环境横幅、版本清单或堆叠的硬件说明。工具偶发失败先尝试一次适当重试或替代只读路径，再报告阻塞。
