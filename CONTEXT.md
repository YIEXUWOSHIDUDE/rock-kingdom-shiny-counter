# 识别与计数领域

本项目只读取主动选择的游戏窗口，并使用 NVIDIA GPU OCR；不读取游戏内存、注入或模拟操作。保底轮次/手动出货语义保持现有行为，不从视觉动画猜测新累计规则。

## 术语和职责

- **识别方案**：赛季目标及默认参数，集中在 `recognition_profile.py`。设置中的用户覆盖值解析为不可变快照；区域预览与实际截图裁剪共用它。
- **捕获样本**：顶部区域的独立像素快照、单调捕获时间和失效代次。像素变化只帮助压缩相邻视觉片段，不证明目标出现/消失。
- **在途样本**：按时序从有界队列合法取出、正在 GPU 推理的唯一样本；只能交付一次结果。排队 TTL 与推理时限分开。
- **横幅命中**：OCR 文本通过关键词和置信度检查，不等于本次需要加一。
- **计数决策**：横幅从允许计数状态进入命中状态时触发一次；连续真实未命中证据使下一次计数重新可用。暂停/捕获中断不凭空创造消失证据。
- **历史事件**：计数决策由 UI 应用到 `CounterState` 并通过 `DataStore` 保存；识别模块不直接改用户文件。

## 模块边界

`BannerRecognitionStream` 是识别协调边界：对外提供 offer / take / observe / invalidate，内部维护裁剪、缓冲、样本身份和去重状态。`RecognitionWorker` 提供截图/GPU 适配器的线程调度及状态信号。`gpu_policy.py` 仍单独管理兼容性政策。

下一赛季应在识别方案模块明确登记目标及迁移规则，不能仅更改 UI 提示语，也不能删除旧方案标识后假定现有数据仍能加载。默认值迁移必须与用户自定义值区分，并用存储往返测试验证。

设计依据和取舍见 `docs/adr/0001-season-recognition-stream.md`，实测与失误复盘见 `DEVELOPMENT.md`。
