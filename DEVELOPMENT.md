# 开发记录与问题复盘

最后更新：2026-09-11
当前开发分支：`agent/fix-exit-v0.4.5`
仓库版本文件：`0.4.5`；退出修复与构建 DLL 路径隔离已完成，新安装包已生成并实际覆盖更新本机，原计数文件保持不变。S4 PR #9 已合并，本次提交用于通过独立 PR 同步退出与构建修复；安装包仍仅本地交付，未发布 Release。现场游戏窗口人工验收仍待完成。

这份文件记录项目需求、重要技术决策、已完成更新、已确认问题、诊断失误和发布检查项。后续修改功能或制作安装包时，应同步更新本文件。

## 1. 项目目标

本项目是洛克王国异色保底计数悬浮工具：

- 截取用户主动选择的游戏窗口。
- 对游戏顶部中央通知横幅进行中文 OCR。
- 当识别到当前赛季配置关键词时自动加一；S4 默认“划破天幕坠落”。
- 保存计数、历史事件和每轮“多少次出”的记录。
- 不读取游戏内存、不注入游戏、不模拟操作。

当前自动计数标准是文字横幅，不是异色宠物图标。异色图标自动记录仍属于后续功能。

## 2. 为什么 OCR 代码里存在硬件检查

EasyOCR 本身并不天然要求 RTX 20 系显卡。当前硬件限制来自本项目的产品决策：

1. 用户明确要求只使用 GPU，不允许降级到 CPU。
2. 目标横幅出现时间较短，CPU OCR 速度不足时容易漏识别。
3. PyTorch 可能因为驱动、CUDA 架构或运行库不匹配而无法使用 GPU。
4. 如果不主动检查，程序可能启动后长期不计数，却不给出明确原因。

因此运行时仍然需要检查：

- `torch.cuda.is_available()`；
- 显卡 CUDA 计算能力；
- 安装包内 PyTorch 支持的 CUDA 架构；
- 实际使用的 GPU 设备。

0.4.1 及更早实现存在职责混合：

- 安装器负责最低硬件要求；
- OCR 引擎又重复输出最低要求；
- 状态栏把“实际故障原因”和“产品最低要求”拼成一条长消息。

这会让用户误以为“识别不到文字”或“抓不到窗口”，实际可能只是 CUDA 没有启动。

后续应把它拆为结构化状态：

- `CAPTURE_READY`：窗口截图正常；
- `CUDA_UNAVAILABLE`：PyTorch 无法使用 NVIDIA CUDA；
- `CUDA_ARCH_UNSUPPORTED`：安装包不包含显卡架构；
- `OCR_READY`：模型和 GPU 均已准备好；
- `OCR_NO_TEXT`：截图成功，但当前横幅区域没有文字；
- `OCR_TEXT_NO_MATCH`：识别到了文字，但关键词未匹配。

0.4.2 已将 Python 侧 GPU 产品策略集中到 `shiny_counter/gpu_policy.py`；
OCR 模块只调用兼容性边界，不再定义最低硬件、固定 Torch/CUDA 版本或发布架构。
安装器和运行时探针继续分别承担安装前检查与真实 CUDA/EasyOCR 验证。

## 3. 已完成更新

### 2026-09-11：0.4.5 源码 PR 同步

用户在本机升级完成后追加授权提交 GitHub PR。确认 S4 PR #9 已合并，最新 `origin/main` 为 `e359c42`，与此前本地 `6033c94` 的源码树一致；从该主线创建独立 `agent/fix-exit-v0.4.5` 分支。提交范围仅为退出修复、构建 DLL 搜索路径隔离、Qt 包自检、相应测试、版本及需求/失误复盘文档。

构建及实机验证结果沿用下节同一份代码的交付证据，不冒充本轮重新执行。PR 同步不包含安装包、模型、运行库、私人数据或 `.scratch/` 报告，不自动合并或创建 Release。

### 2026-09-11：0.4.5 关闭退出修复与本机交付

已确认主悬浮窗采用 `Qt.Tool`，运行时 `WA_QuitOnClose=false`；× 只触发窗口关闭，清理完成后未显式结束 `QApplication`。即使未启动 OCR 或快捷键，也能复现窗口消失、Qt 主循环仍运行的现象。

最小修复：托盘“退出”统一进入 `self.close`，`closeEvent` 在原有保存、线程停止、托盘隐藏及接受关闭后显式调用 `QApplication.quit()`；OCR 停止超时仍拒绝关闭，不强杀线程、不改变 GPU-only、识别方案或数据格式。

退出子进程检查先在旧代码得到窗口隐藏、主循环需兜底退出 99 的失败，修复后正常返回 0。新增检查覆盖 ×、托盘，以及 OCR 忙碌时两种入口拒绝关闭并可重试；退出/UI 相关 16 项通过。独立只读复核未发现本次修改的阻断问题。最终干净构建运行 124 项源码测试（7.478 秒）和 16 项 C# 安装器检查通过，不能将替身 worker 检查等同于真实 GPU 工作期间退出验收。

首次构建的真实 GUI 检查另发现 `QtCore` DLL 导入失败（退出码 2），尽管 GPU 与旧包自检已通过。已定位 PyInstaller 从宿主工具 PATH 误收 Poppler `icuuc.dll`，其缺少 Qt 所需的 20 个无版本后缀 ICU 导出，并伴随 libheif 运行库混入。Qt 自身文件与旧版哈希相同；这属于打包搜索路径污染，不是用户缺驱动或退出修复引发的新 UI 逻辑问题。失败构建未交付、未覆盖本机。修正为仅在 PyInstaller 子流程使用发布 Python 与 Windows 系统搜索路径，结束后恢复环境；包自检补充真正的 QtCore/QtGui/QtWidgets 导入，不再用 PySide6 根包导入代表界面可启动。

最终交付：

- 安装包：`release/exit-fix-0.4.5/out/RockKingdomShinyCounter-Setup-v0.4.5-GPU.exe`，3,020,562,452 字节；SHA-256：`d0a5e89adbff614c8c8e4e4f3e26bb170447067fe36f841246df451ac5b54512`。非 candidate，保留旧 0.4.4 安装包。
- 新构建清单不再引用宿主 Poppler/libheif DLL；实际冻结 Qt 界面模块导入与 CUDA/EasyOCR 推理通过，仍只用 GPU。实测设备为 RTX 3070 Laptop、驱动 610.62；不是跨显卡验收。
- 最终包内 2,935 个文件条目与清单集合一致，版本与说明正确；指定私人数据路径及常见私人媒体/日志扫描通过，冲突的根 `icuuc.dll` 未打包。
- 正式 `/SILENT` 本机升级返回 0，实际安装及卸载注册版本均为 0.4.5，主 EXE 哈希与新构建一致；真实 `data.json` 已备份且升级前后 SHA-256 相同。原桌面快捷方式指向已更新安装目录，本次未发现需要停止的旧计数器进程。
- 干净构建 EXE 与实际安装 EXE 均在私有桌面、隔离数据下正常启动，并通过与 × 相同的关闭事件退出；分别约 0.094 秒、0.084 秒，退出码 0，未强杀，隔离计数/历史保持不变。未启动真实用户配置的 GUI，也未把无选定游戏窗口的关闭检查冒充游戏捕获或真实 GPU 忙碌退出验收。
- 详细报告及私人备份仅存于 `.scratch/exit-fix/`；本地交付阶段未提交 GitHub、上传安装包或发布小红书。后续源码 PR 同步见上节，不包含这些私人文件或安装包。

### 2026-09-09：本机 0.4.3 → 0.4.4 覆盖升级占用恢复

用户实际升级报“另一个程序正在使用此文件”。确认两个旧计数器进程仍来自实际安装目录，其中一个需要管理员权限才能结束。备份真实计数文件后，仅结束已核实的旧计数器进程（高权限进程经用户 Windows UAC 批准），不结束游戏、不修改安全设置，再使用原 0.4.4 安装包实际覆盖升级。

安装器返回 0，实际安装目录和卸载注册版本均为 0.4.4，安装前后 `data.json` SHA-256 完全相同；未跳过硬件、载荷完整性或 GPU OCR 验证。此结果补充了本机真实旧安装升级的证据，不扩展为其他机器或故障回滚验收。

用户要求优先解决当前安装，已暂停代码修复和额外测试。本次没有修改应用退出逻辑或安装器：此前记录的“关闭窗口后进程可能残留”仍是已知缺口，不应把本次解除占用描述成代码缺陷已修复。恢复记录和备份仅存于 `.scratch/install-recovery/`，不提交私人数据。

### 2026-09-09：S4 源码 PR 更新

用户要求先更新 GitHub，沿用已有 PR 工作流。本次提交包含 S4 识别方案、连续提示缓冲修正、配置迁移、0.4.4 包内自检与配套测试，并同步项目规则、领域说明和开发复盘。基线为已合并 PR #8 的 `origin/main`（`fc12572`，树与本地原基线 `3f873ac` 一致）。

仅提交经过检查的源码、测试和 Markdown 文档，不包含 `.scratch/`、`release/`、模型、安装包或私人素材。此流程不包含合并 PR、创建 Release、上传附件或自动更新已安装程序。安装验证的已通过范围和待验收项见下一节。

提交前重新执行 `release\.venv-cuda128\Scripts\python.exe -B -m unittest discover -s tests -q`：121 项通过（4.198 秒）；`git diff --check` 通过。此次没有把前轮 GPU/安装测试写成重新执行，也没有覆盖真实计数数据。

### 2026-09-09：S4 0.4.4 本地安装包（未上传）

用户授权“来吧”后，使用已有锁定 wheelhouse 和离线模型制作新包；该打包阶段当时没有提交、推送或发布 GitHub，也没有覆盖真实安装或用户数据。

- 交付文件：`release/s4-0.4.4/out/RockKingdomShinyCounter-Setup-v0.4.4-GPU.exe`，3,025,703,785 字节（约 3.03 GB / 2.82 GiB）。SHA-256：`c61ff102aac4f4180490a351cb6fa3713d94c90879e8ed3e2d0a216cf11d9cf4`。
- `build-release.ps1` 新增独立产物目录参数；本次使用 `release/s4-0.4.4`，未跳过测试或本机 GPU 探针、未使用复用环境捷径。离线解析所有已锁依赖，CUDA OCR 无 CPU 回退。
- 安装包自检新增当前赛季方案、合成旧数据迁移和完整识别协调层的合成时序验证；明确标记没有使用真实游戏或 GPU OCR 输入。固定 GPU 探针图片改为当前 S4 关键词，先观察旧词断言失败再修正。
- 最终源码测试 121 项通过（3.927 秒），`git diff --check` 通过；构建中的 C# 兼容性和原子替换/回滚测试通过。
- 冻结 staging 与实际隔离安装目录的包自检、CUDA 自检均通过：RTX 3070 Laptop GPU、驱动 610.62、Torch 2.11.0+cu128，实际使用 `cuda` 读出“划破天幕坠落”。安装后固定图片单次推理 827.10ms；这是冷启动固定图片检查，不代表游戏采样延迟。
- 实际安装器 `/SILENT /TEST` 在 `.scratch/s4-installer/installed` 成功安装并返回 0：没有创建注册表项或快捷方式；仍执行载荷逐文件完整性校验和真实 CUDA OCR 验证。只替换模拟旧标记目录，不冒充真实 0.4.3 升级或故障回滚实测。
- TEST 模式跳过的静态硬件检查由同一份 `Compatibility.cs` 编译的隔离辅助程序执行 Detect/Evaluate，6 通过、0 警告、0 失败；这不是完整安装器界面操作验收。
- 最终 EXE SHA-256 与附加载荷 SHA-256 重算匹配清单，载荷 2940 项与逐文件清单严格一致；常见媒体/日志/私有数据路径扫描没有发现禁止项（允许内置 OCR 探针图片和 Python 运行时 ZIP），不能据此宣称通用内容级隐私审计。
- 私有、不切换的 Windows 桌面上实际启动安装后的 GUI，仅使用隔离的旧配置样本：关闭保存后关键词迁移为 S4，原 61 次和完整历史保持相等。WM_CLOSE 后 5 秒仍未退出，由测试脚本清理其自有进程；本项仅为“启动及关闭保存验证”，不算正常退出验证。不要把隐藏悬浮窗等同进程退出；升级前建议使用托盘“退出”。
- 旧 `release/out` 0.4.3 候选安装包保留，前后 SHA-256 一致：`36b90df71e6df7f04c3f5d074d02e45f7816f2e0e86858916c674239c2a8e2c3`。

仍未验证：实际交付版本在当前真实游戏窗口中的计数、多显卡/多机覆盖、真实旧版完整升级、完整安装器交互及正常 GUI 退出。此包默认适配已确认的 S4 横幅；自定义关键词保留，必要时用户自行改为“划破天幕坠落”。

完整本地证据位于 `.scratch/s4-installer/`，包括 `installed-package.json`、`installed-runtime.json`、`delivery-verification.json` 和硬件辅助报告；私人媒体、样本计数数据、测试辅助程序均不进入发布载荷。

### 2026-09-09：S4 识别方案与计数时序实现记录

对应基线：`3f873ac`。以下记录源码阶段的实现与验证；当时修改尚未提交、尚未制作安装包，后续 0.4.4 打包和源码 PR 交付记录见上文。

已确认的输入：用户提供的 2560×1600 PC 录屏出现“精灵藏进星星，划破天幕坠落！”。旧赛季文字不再出现，不应继续把旧关键词失配当作待验证的硬件假设。当前顶部区域 `(0.28, 0.10, 0.72, 0.27)` 覆盖样本；原始时间戳下可保守读字段为 22.500–25.601 秒，约 3.10 秒，不把前后的动画时间算作稳定 OCR 时间。

实现：

- `recognition_profile.py` 集中赛季标识、名字、关键词、ROI、置信度和采样/门控默认值，以及旧默认配置迁移。`AppSettings.recognition_profile()` 生成带用户覆盖值的不可变快照，实际裁剪与截图预览使用同一配置。
- 仅“无方案标识且等于旧默认词”的记录迁移；自定义关键词和其他参数、计数历史保留。未知方案报告明确错误，不当作损坏数据创建备份，也不覆盖原文件。旧格式无法识别“用户手填与默认完全相同的词”，这是迁移假设。
- `BannerRecognitionStream` 收口裁剪、帧队列、在途有效性、关键词匹配及一次提示只计一次的状态；Worker 只协调窗口适配器、GPU 引擎、节流与信号。启动时冻结设置，避免运行中一半新配置、一半旧配置。
- 缓冲改为按时间消费相邻视觉片段的真实代表帧，取消“取全局最大变化帧并清空整批”。保留自定义进入/退出所需的真实确认样本；变化分数不作为计数证据。
- 区分最多 4 秒的队列保留与最多 4 秒的单次在途推理窗口；只接受已发出样本一次。暂停、捕获失败和关闭会使旧结果失效，但不无条件重新布防造成重复计数。
- UI 的 `OCR_MATCH` 表示横幅仍显示；只有 `COUNTED` 表示实际触发计数 +1。

本次纠正的开发失误：

1. 旧缓冲回归测试把“先选变化最大帧”视作正确行为，没有测试“横幅 A → 两帧真实空白 → 横幅 B”积压后仍应计两次，也遗漏无后续 offer 时的过期检查。现在通过完整 buffer→matcher→gate 行为断言覆盖。
2. 初版 FIFO 修正仍在 OCR 完成后检查截图年龄，动态背景 20fps 生产、5fps 消费时会让所有结果持续过期。独立复核内存样本末 20 次接受率原为 0/20；分离排队和在途时限后为 20/20。
3. 固定最多 3 张代表帧会破坏用户自定义较大的确认帧数；现由识别方案传入所需真实样本数，避免压缩阶段预先删掉必要证据。总容量和过期限制仍优先。
4. 旧状态将每次关键词命中都写成“计数 +1”，与防重复门控不符。现按真实计数决策输出。

验证与限制：

- 按 diagnose / TDD 先记录失败再修正，并做独立只读复核；没有把合成文本测试当作 GPU 准确率。
- 源码测试命令：`release\.venv-cuda128\Scripts\python.exe -B -m unittest discover -s tests -q`。最终完整 suite：117 项通过（3.902 秒）；两项新增 Worker 生命周期测试另连续重复 5 轮通过。`git diff --check` 通过。
- 实际 GPU 回放使用 RTX 3070 Laptop GPU，Torch 2.11.0+cu128，检测及识别模型均为 `cuda:0`，只读已有模型且禁止下载。完整 33.55 秒视频计数 1，测试数据持久化为 1；20–28 秒片段重复两次，计数和测试历史均为 2。第二项是人工拼接同一事件，不是两次独立实机事件。
- 完整回放 182 次 OCR，中位数 64.27ms、最大 405.98ms；双片段 98 次 OCR，中位数 76.12ms、最大 136.57ms。曾出现约 3.95 秒排队延迟，因此不能宣称实时无延迟。
- 回放通过本次 Worker、帧队列、GPU OCR、计数信号和临时 DataStore，只替换 Win32 截图边界。没有启动真实计数悬浮窗、改用户计数、实测其他 GPU、验证安装器或当前交付 EXE。
- 暂停期间无法看到消失证据、缓冲溢出及推理超时仍可能保守漏计；这次不改变流星雨累计、保底池、出货/重置语义或 GPU 门槛。

原始私人视频、回放脚本和完整 JSON 只保留在 `.scratch/s4-implementation/` 及之前的样本诊断目录，禁止进入发布载荷。

### 2026-07-17：初始 CUDA OCR 版本

对应提交：`dfc266c`

- 建立悬浮计数器。
- 支持选择 Windows 游戏窗口。
- 使用 EasyOCR 中文模型识别画面。
- 使用关键词匹配和出现/消失门控避免重复计数。
- 保存计数和基本设置。

### 2026-07-17：针对顶部通知横幅优化

对应提交：`dea068c`

- 根据实际游戏截图将 OCR 区域缩小到顶部中央。
- 默认关键词改为“写进了童话里”。
- OCR 目标间隔调整为 200ms。
- 横幅进入门槛调整为一帧，以提高短暂横幅命中率。
- 保留连续消失后重新布防，避免同一横幅重复加一。

注意：200ms 是扫描循环的目标间隔，不代表固定每秒五次。一次 OCR 推理如果超过 200ms，实际间隔就是推理耗时。

### 2026-07-18：提高悬浮窗文字对比度

对应提交：`01d058d`

- 提高悬浮窗文字颜色对比度。
- 改善游戏亮色背景下的可读性。

### 2026-07-19：GPU-only CUDA 13 安装包

对应提交：`c813984`

- 明确不提供 CPU 回退。
- 增加 NVIDIA CUDA 设备和架构检查。
- 为 RTX 50 系准备 CUDA 13 兼容安装包。
- 安装包内置 EasyOCR 中文模型。
- 模型缺失时优先使用 ModelScope、GitCode 等国内来源。
- 当前项目最低策略为 RTX 20 系、4GB 显存和指定版本以上的 NVIDIA 驱动。

这些是当前安装包策略，不应表述成 EasyOCR 的通用最低要求。

### 2026-07-24：锁定悬浮窗与保底轮次

对应提交：`ee9d15f`

- 默认锁定悬浮窗位置，减少刷取过程中误触拖动。
- 支持手动结束一轮并记录本轮次数。
- 为后续异色图标自动结束轮次预留来源字段。

### 2026-07-24：历史记录直观化

对应提交：`8578ba8`、`c3bfe39`

- 历史页面增加“第几轮、多少次出、结束时间、记录方式”。
- 回填旧数据中的重置记录。
- 旧数据没有当时保底上限时，不推测“提前多少次”。

### 2026-07-24：修复设置页浅色文字

对应提交：`555fd4f`

- 修复对话框和设置页白色文字在浅色背景上看不清的问题。

### 2026-07-24：全局快捷键改为按需开启

对应提交：`92afa23`

- 默认不注册全局快捷键。
- “补一、撤销、暂停”只保留界面按钮。
- 全局快捷键放入高级设置。
- 单个快捷键注册失败只显示状态，不弹窗打断启动。
- 点击穿透默认关闭。
- 开启点击穿透时，要求存在系统托盘恢复方式或关闭穿透快捷键。

这项修改用于解决不同用户电脑上 `Ctrl+Alt+Down`、F10 等快捷键占用问题。

### 2026-07-24：安装器 0.4.1 与兼容性检测

对应提交：`0929250`、`ae40e4b`

- 安装器版本更新为 0.4.1。
- 安装前检查 Windows 版本、系统位数、磁盘、NVIDIA GPU、显存、驱动和 CUDA 计算能力。
- 优先使用 `nvidia-smi`，失败时通过 WMI 读取部分信息。
- 明确绿色、黄色警告和红色不兼容结果。

当前生成安装包：

```text
RockKingdomShinyCounter-Setup-v0.4.1-GPU.exe
SHA256: 22F5CC02D4AA372517B53890BCB94C0E0273625B68FCB6B3C47F481C56FAF7FB
```

## 4. 已确认的设计问题

### 4.1 选择窗口后只保存标题

现状：

- 窗口选择器已经拿到 HWND。
- 保存设置时却只保存完整窗口标题和客户区尺寸。
- OCR 每次截图前重新按标题枚举窗口。

风险：

- 游戏标题变化后找不到窗口；
- 同名窗口可能匹配错误；
- 最小化窗口会被枚举逻辑提前排除；
- 切换全屏、场景或启动器后容易失去绑定。

正确方向：

- 优先保存并使用 HWND；
- 同时记录 PID 和窗口类名；
- HWND 失效后按 PID/类名恢复；
- 标题只用于展示和最后兜底；
- 最小化时等待恢复，不应伪装成“找不到窗口”。

0.4.2 修复：保存 HWND、PID、窗口类和进程路径；优先验证 HWND
对应的进程身份，失效或被复用后按进程路径/窗口类恢复，标题仅作最后兜底。

状态：已修复并覆盖标题变化、进程重启和 HWND 被其他进程复用的测试。

### 4.2 首次截图异常捕获范围过窄

选择窗口确认后，程序只捕获 `CaptureError`。以下异常可能直接从 Qt 槽函数逸出：

- `ModuleNotFoundError`；
- `mss.exception.ScreenShotError`；
- PyWin32/OSError；
- 打包依赖缺失；
- 其他运行时异常。

无控制台安装版发生这类错误时，用户看到的只是选择框关闭，底部仍提示“请先选择窗口”。

正确方向：

- 在首次截图边界捕获并显示所有预期运行时异常；
- 写入本地诊断日志；
- 错误中包含阶段、窗口标题、HWND、尺寸和异常类型；
- 不应静默返回。

0.4.2 修复：首次截图捕获所有异常，显示
`CAPTURE_SELECTION_FAILED`，并把 HWND、PID、窗口类、进程路径、尺寸和
traceback 写入轮转诊断日志。

状态：已修复。

### 4.3 没有截图预览和阶段化诊断

当前“查看文字”只能显示最近一次 OCR 文本，而且空帧会覆盖之前结果。用户无法判断：

- 是否选中了正确窗口；
- 截图是不是黑屏；
- 顶部横幅裁剪位置是否正确；
- OCR 是否来得及处理；
- 是否识别到了文字但关键词不匹配。

正确方向：

- 增加“测试截图”；
- 显示整窗缩略图和 OCR 裁剪框；
- 保存最近一次非空 OCR 结果；
- 显示最近扫描耗时、设备、状态码和关键词匹配结果。

0.4.2 修复：新增“测试截图”与红色 OCR 裁剪框，保留最近一次非空 OCR
文字；状态栏显示 GPU、单次推理耗时、缓冲等待、匹配结果和结构化错误码。

状态：已实现。

### 4.4 OCR 速度没有在目标显卡上形成基准

当前设置中的 200ms 只是循环下限。如果 EasyOCR 一次推理耗时 800ms，750ms 横幅仍可能被完全错过。

正确方向：

- 在 RTX 20、30、40、50 系代表设备上记录首次推理和稳定推理耗时；
- 截图线程与 OCR 线程解耦；
- 每 50 ms 捕获一次，变化帧最多保留 4 秒/96 帧，覆盖约 750 ms 的短提示和较慢 GPU OCR；
- GPU OCR 从候选帧中优先处理变化最大的帧；
- UI 显示单次 GPU OCR 和排队缓冲耗时。

状态：架构与自动测试已完成；各代 RTX 的真实游戏基准测试仍未完成。

### 4.5 构建流程不可复现

0.4.1 没有提交固定的 PyInstaller `.spec` 和完整构建脚本。不同安装包可能因为临时构建命令、缓存或复用目录而包含不同模块。

已观察到：

- 本地生成的 0.4.0 可执行文件归档中能找到 `mss`；
- 当前检查的 0.4.1 生成物中没有在主归档、外部文件或 `base_library.zip` 中找到 `mss`；
- 但另一位用户的视频已经运行到 CUDA 检查错误，说明其环境成功越过了 `Win32Capture` 初始化；
- 因此不能仅凭版本号断言所有 0.4.1 用户缺少同一个模块，必须先比对安装包 SHA256、实际可执行文件路径和运行环境。

正确方向：

- 提交唯一的 `.spec` 和一键构建脚本；
- 每次使用干净目录构建；
- 固定并打印依赖清单；
- 构建后自动检查 `mss`、PyWin32、NumPy、EasyOCR、Torch 和 PySide6；
- 在干净 Windows 环境安装并完成真实窗口截图冒烟测试。

0.4.2 修复：

- 新增唯一的 `packaging/RockKingdomShinyCounter.spec`；
- 新增 `build-release.ps1`，每次清理独立构建/暂存/输出目录；
- PyTorch 固定为 2.13.0 官方 `cu130`，TorchVision 固定为 0.28.0；
- 构建门禁要求 CUDA 13.0 且包含 `sm_75`、`sm_86`、`sm_120`；
- 版本由根目录 `VERSION` 同时嵌入 Python 应用和 C# 安装器；
- 生成应用文件哈希、构建环境、确定性 ZIP 和最终安装包 SHA-256。

状态：流程已补齐；正式发布仍需在支持 CUDA 13 的构建/测试机执行真实探针。

0.4.3 兼容性调整：

- 将单一 GPU-only 运行库改为官方 PyTorch 2.11.0 `cu128` / TorchVision 0.26.0；
- 继续要求 wheel 同时包含 `sm_75`、`sm_86`、`sm_120`，仍覆盖 RTX 20/30/40/50；
- Windows 最低 NVIDIA 驱动从 580.88 降为 CUDA 12.8 对应的 570.65；
- 没有增加 CPU 回退，CUDA 不能启动时仍直接停止识别。

### 4.6 安装器对“驱动信息未知”放行过宽

0.4.1 安装器会检查 NVIDIA 显卡和驱动，但不代表所有检查失败都会阻止安装。

当前逻辑：

- 只有存在红色 `Fail` 项时，`CanInstall` 才会变为 `false`；
- `nvidia-smi` 成功并明确读到驱动低于 580.88 时，会标记为红色并阻止安装；
- `nvidia-smi` 不可用或查询失败时，程序回退到 WMI；
- WMI 只能确认存在 NVIDIA 显卡，读取到的驱动、显存和计算能力被标记为不可靠；
- “无法读取驱动”和“无法读取计算能力”被标记成黄色 `Warning`；
- 黄色警告仍然允许点击安装。

当初这样设计是为了避免用户电脑上的 `nvidia-smi` 路径、权限或查询字段不兼容造成误拦截，但对于“只允许 GPU、完全不提供 CPU 回退”的程序，这个兜底过于宽松。结果是安装器允许继续，应用启动后才由 `torch.cuda.is_available()` 发现 CUDA 实际不可用。

即使驱动版本检查通过，也不能证明安装包内的 PyTorch 一定能加载 CUDA。以下问题只能在运行安装包自身运行库后确认：

- PyTorch CUDA DLL 加载失败；
- 显卡架构不在当前 Torch 构建中；
- 笔记本独显被禁用或进程未获得独显；
- 驱动安装损坏；
- 安装包依赖缺失或版本不一致。

正确方向：

- GPU-only 模式下，“驱动未知”不应直接视为可安装；
- 安装器解压到暂存目录后，安装前运行暂存程序的 `--write-runtime-info`；
- 必须确认 `cuda_available=true`、设备架构受支持、OCR 模型存在且校验通过；
- 运行时验证失败时阻止替换旧版本，并显示实际 Torch、CUDA、GPU、驱动和异常；
- 如保留黄色警告安装，必须要求用户明确确认“CUDA 尚未验证，程序可能无法工作”，不能显示成普通检测通过。

0.4.2 修复：

- 所有兼容性项目必须明确为 Pass，未知 GPU/驱动/计算能力也会停止安装；
- 新版本先解压到暂存目录；
- 暂存 EXE 强制验证固定 PyTorch CUDA 13、CUDA 张量运算、同步、模型 SHA-256、`gpu="cuda"` 的 EasyOCR Reader 和一次真实推理；
- 任一步失败都不会删除或替换现有版本；
- EasyOCR 不再使用会静默 CPU 回退的 `gpu=True`。

状态：已修复并有安装事务回滚测试。

## 5. 用户问题复盘

### 5.1 快捷键占用

现象：

- 启动即提示 `Ctrl+Alt+Down` 被占用；
- 部分用户没有 F10 或不理解全局快捷键用途。

原因：

- 将非核心操作默认注册为全局快捷键；
- 默认配置无法适配不同软件和键盘。

处理：

- 已改为默认关闭；
- 点击穿透恢复能力仍保留保护。

### 5.2 字体颜色不可读

现象：

- 设置页和历史页出现白字配浅色背景。

原因：

- 悬浮窗深色样式影响了普通对话框；
- 没有逐页做视觉验收。

处理：

- 已修复对话框样式；
- 后续发布必须检查浅色和深色背景。

### 5.3 保底历史不直观

现象：

- 用户要求历史中直接看到“第几轮、多少次出”；
- 初版只记录详细事件，轮次表为空；
- 用户误以为旧记录丢失。

原因：

- 数据模型先记录事件，没有先定义用户需要查看的业务结果；
- 旧数据迁移没有和界面更新同时完成。

处理：

- 已增加轮次记录；
- 已对旧重置事件做有限回填；
- 无法从旧数据可靠恢复的信息明确标注，不进行猜测。

### 5.4 误触拖动计数器

现象：

- 刷取过程中容易误触拖动悬浮窗。

处理：

- 已增加并默认启用位置锁定。

### 5.5 第一段窗口选择视频

视频事实：

- 用户确实选中了“洛克王国：世界”；
- 随后点击了 OK；
- 选择框关闭后，状态仍为“请先点击选窗口”。

诊断失误：

- 第一次使用稀疏抽帧观察时，错误判断成用户没有点击窗口行；
- 用户指出后安装 FFmpeg，按原始帧率复核，确认用户已经正确点击。

目前结论：

- 问题发生在确认之后、设置写入之前；
- 可能是列表项数据取回失败，也可能是首次截图抛出未捕获异常；
- 没有目标电脑日志前，不能继续把某一个可能原因写成事实。

需要改进：

- 视频问题必须查看关键操作前后的连续原始帧；
- 不应根据单帧光标位置判断是否点击；
- 程序必须提供阶段化日志，减少依赖手机录像猜测。

### 5.6 第二段横幅未计数视频

视频事实：

- 游戏窗口和悬浮窗均正常显示；
- “小加尔把精灵奇遇写进了童话里”横幅持续数秒；
- 计数仍为 0；
- 状态栏显示“未检测到可用的 NVIDIA CUDA……”。

结论：

- 不是横幅时间太短；
- 不是关键词没匹配；
- OCR 引擎在扫描前已经因为 CUDA 不可用而停止；
- 需要检查该用户的 `nvidia-smi`、显卡型号、驱动、独显启用状态和 PyTorch CUDA 运行库。

诊断失误：

- 一度把两位 0.4.1 用户归为同一个安装包/窗口捕获问题；
- 第二段视频证明两者处于不同失败阶段。

需要改进：

- 不再按“版本相同”合并故障；
- 必须先依据状态码确定失败阶段；
- CUDA 错误应突出实际原因，不应只显示通用最低要求。

### 5.7 RTX 4070、2560×1600 用户视频

连续帧确认：

- 用户已经选中第五行“洛克王国：世界（2560×1600）”并点击确认；
- 状态先进入 `CAPTURE_READY`，证明窗口句柄有效且首帧截图成功；
- 随后出现 `CUDA_UNAVAILABLE`，OCR 尚未开始，横幅出现时自然不会计数。

本次诊断失误：

- 初看窗口列表时再次把问题归因于选择项，用户纠正后才按连续帧复核；
- 状态栏把状态码、设备、耗时、缓冲和日志路径塞在一行，用户无法判断真正失败阶段；
- 0.4.2 为 RTX 50 系直接采用 `cu130`，却把所有 RTX 20/30/40 用户的最低驱动一并抬到 580.88，兼容范围过窄。

0.4.3 处理：

- 改用同时支持 Blackwell 的官方 `cu128`，最低驱动降至 570.65；
- 通过 `nvidia-smi` 读取实际显卡名和驱动，分别提示“驱动过旧”或“CUDA 运行库无法启动”；
- 底部只显示“已截图、OCR 已就绪、等待横幅、读到但未匹配、驱动需升级”等操作结论；
- 原始状态、耗时和诊断细节保留在悬停提示及日志中；
- 仍严格 GPU-only，不尝试 CPU 识别。

构建验证：

- 已生成 0.4.3 CUDA 12.8 candidate，固定 `torch 2.11.0+cu128`；
- wheel 架构实测包含 `sm_75`、`sm_86`、`sm_120`；
- 79 个 Python 测试与 16 个 C# 安装器测试通过；
- 冻结包依赖/离线资源探针通过；
- 构建机驱动 561.09 低于 570.65，因此未把本机 GPU 探针伪装成通过，仍需在合格驱动的 RTX 目标机安装验收。

### 5.8 2026-09-09：安装失败处理范围扩大，恢复使用被排在后面

用户要求解决 0.4.4 安装时的文件占用错误，首要目标是让本机完成升级。此前已记录关闭计数器窗口后进程可能残留，应先核实安装目录对应的旧进程；实际却先安排了退出逻辑修复、安装器检查和回归验证，把现场恢复扩大成代码改造。用户提醒后才调整顺序。这是处理优先级和范围判断的失误，不是用户需求不清楚。

规则套用复盘：`AGENTS.local.md` 的相关测试、实际交付验证要求，以及 `diagnose`、`tdd` 中的回归测试流程，不等于每次现场安装故障都必须先做大范围测试。本次错误在于未区分“恢复当前使用”和“修改代码交付”。准确记录：扩大范围的工作已安排，但随后暂停，未实施该代码修复或运行新增测试，不能写成已完成大量测试。

本次有效处理顺序：核实占用进程及程序路径 → 备份计数数据 → 仅关闭已确认的旧计数器进程（必要时经用户 UAC 授权）→ 用原安装包重试升级 → 核对退出码、实际版本及数据完整性。安装返回 0，版本为 0.4.4，计数文件哈希未变；未结束游戏，未跳过硬件、载荷完整性或 GPU OCR 验证。

后续原则：先采用安全、直接相关的措施恢复使用；恢复成功即报告结果，并单独记录仍待修复的代码缺陷。不自动扩展为架构整改、重新打包或全量测试。若仍无法恢复，或用户要求根治，再定位代码缺陷、实施最小修复并做与风险相称的验证。此次解除占用不代表“关闭窗口后进程可能残留”的代码问题已经修复。

## 6. 未完成或尚未支持

- AMD 显卡和 ROCm：未支持。
- CPU OCR 回退：按当前需求明确不支持。
- 异色宠物图标自动记录：未实现，目前依靠文字横幅自动加一，出货轮次手动重置。
- 应用内自动更新：未实现，用户仍需下载新安装包。
- NVIDIA 录制/Overlay 兼容性：没有完成系统化验证。
- 多显示器、不同 DPI、独占全屏、HDR：没有完成完整兼容性矩阵。
- 真实游戏端到端自动化测试：未实现。

## 7. 发布前强制检查清单

### 构建

- [ ] 使用仓库内固定 `.spec` 和构建脚本。
- [ ] 从干净构建目录开始，不复用旧 `dist`/`build`。
- [ ] 输出 Python、PyInstaller 和所有直接依赖版本。
- [ ] 检查安装包 SHA256 并记录在本文件或发布说明。
- [ ] 验证安装包确实包含 `mss`、PyWin32、NumPy、EasyOCR、Torch、PySide6 和 OCR 模型。

### 安装

- [ ] 在没有开发环境和全局 Python 包的干净 Windows 用户环境安装。
- [ ] 验证首次启动不依赖联网下载模型。
- [ ] 验证卸载、覆盖安装和数据保留。
- [ ] 验证安装器显示版本与程序实际构建一致。

### 窗口捕获

- [ ] 选择窗口后立即显示“捕获成功”和缩略图。
- [ ] 窗口化、无边框、多显示器和不同 DPI 均能截图。
- [ ] 最小化后显示等待，恢复后自动继续。
- [ ] 修改分辨率后给出明确提示。
- [ ] 首次截图异常能够弹出并写入日志。

### CUDA 与 OCR

- [ ] RTX 20/30/40/50 系至少各验证一台代表设备或可信测试环境。
- [ ] 记录 `torch`、CUDA 构建、设备名、计算能力和支持架构。
- [ ] `nvidia-smi` 正常时，程序能给出一致的 CUDA 诊断。
- [ ] 横幅出现时能够看到截图、裁剪区域、OCR 文本和匹配结果。
- [ ] 记录首次推理和稳定推理耗时。
- [ ] 验证目标横幅至少连续触发多次，不漏计、不重复计数。

### 界面

- [ ] 设置、历史、窗口选择和错误提示文字均清晰可读。
- [ ] 默认不注册全局快捷键。
- [ ] 默认关闭点击穿透。
- [ ] 默认锁定位置，解锁状态明确。
- [ ] 长错误信息可以完整查看或复制。

## 8. 诊断纪律

先区分现场恢复与代码修复，按失败阶段选择必要检查，不把下列项目全部作为恢复使用的前置条件。安装文件占用问题优先按 5.8 节处理；恢复后保留未修复缺陷的记录，不以测试数量代替解决结果。

后续处理用户故障时按以下顺序：

1. 获取安装包 SHA256、程序路径和版本。
2. 获取状态栏完整文字，不用“识别不了”代替真实错误。
3. 判断失败阶段：窗口枚举、首次截图、CUDA、OCR、关键词、计数门控。
4. 获取“测试截图”和最近一次非空 OCR 结果。
5. 对视频按原始帧率查看关键操作，不使用稀疏抽帧直接下结论。
6. 同版本的不同用户也必须分别诊断，除非已有相同错误和相同复现证据。
7. 将结论分为“已确认”“最可能”“待验证”，不把推断写成事实。
8. 现场恢复需验证原失败操作成功及用户数据保留；代码修复需针对原问题做回归验证，涉及安装或发布时再执行相应干净环境和实际交付包验证。不得把本机恢复成功扩大为跨设备兼容或完整发布验收。
